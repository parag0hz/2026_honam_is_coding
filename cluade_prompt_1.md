
Claude가 응답을 완료했습니다

15초 동안 생각함
15초 동안 생각함
좋습니다. 실제 지금 상태(사진 분석 + 음성 + 폴백 체인 다 작동) 기준으로 다시 썼습니다. 제목은 프로젝트/코드 전체를 다시 리네이밍하기엔 지금 리스크가 크니, "Fruit-Harness AI"는 그대로 두고 PPT와 동일한 정식 타이틀을 부제로 붙이는 방식으로 했습니다 — 제출 자료(PPT·README) 간 이름이 완전히 일치해야 심사위원이 헷갈리지 않습니다. 다른 제목을 염두에 두신 거면 말씀해주세요.

markdown
# Fruit-Harness AI

### 하네스 엔지니어링 기반 이상기후 대비 과수 적산온도·생육 안전 솔루션

전북 노지 과수 농민(61세 최복남 페르소나)을 위한 환상박피(girdling) 시공 타이밍 판단 도우미.
이상기후로 기존 경험칙이 어긋나는 상황에서, 기상/토양/수액 데이터와 과일 사진(Vision LLM)을 바탕으로
"지금 박피해도 되는지"를 판단하고, 위험할 경우 근거와 함께 경고를 보여주는 해커톤 MVP입니다.

> **아키텍처**: React(Vite) 프론트엔드가 FastAPI 백엔드의 REST API를 호출해 (1) 가상 센서 데이터 기반
> 규칙 기반 위험도 판단과 (2) OpenRouter Vision LLM 기반 과일 사진 분석 결과를 받아, 두 결과를 합친
> 최종 권고 문장을 대시보드에 보여줍니다. 판정 문장은 음성(브라우저 내장 TTS)으로도 들을 수 있습니다.

> ⚠️ **가상 데이터 안내**: 실제 하드웨어 센서가 없으므로 `backend/simulator.py` 가 생성하는
> 기온/토양수분/수액지수 등 센서 데이터는 데모용 가상(mock) 데이터입니다. 과일 사진 판독은
> 실제 Vision LLM(OpenRouter) 호출 결과이며, `demo_data/`에는 검증용 실제 사진·데이터를 포함했습니다.

## 기술 스택
- 백엔드: Python + FastAPI + uvicorn
- 프론트엔드: Vite + React (JavaScript) + Tailwind CSS + recharts
- LLM 연동: OpenRouter (`backend/llm_client.py`)
  - 비전 모델: `anthropic/claude-opus-5` → `openai/gpt-5.6-sol` 자동 폴백 체인
  - API 키 미설정/호출 실패/타임아웃 시에도 항상 규칙 기반 판단 결과로 안전하게 폴백

## 실행 방법

### 백엔드 (프로젝트 루트에서 실행)
```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r backend\requirements.txt
uvicorn backend.main:app --reload
```
기본 주소: http://127.0.0.1:8000 (API 문서: http://127.0.0.1:8000/docs)

### 프론트엔드
```powershell
cd frontend
npm install
npm run dev
```
기본 주소: http://localhost:5173 (백엔드 CORS 설정이 이 포트를 허용합니다)

### 환경변수
`.env.example` 을 참고해 프로젝트 루트에 `.env` 파일을 만드세요.
`OPENROUTER_API_KEY`를 설정하면 실제 Vision LLM 과일 사진 분석이 동작하며,
키가 없어도 나머지 기능(위험도 판단)은 그대로 동작합니다.

## API 엔드포인트
| Method | Path | 설명 |
|---|---|---|
| GET | `/api/health` | 서버 상태 확인 |
| GET | `/api/simulate-sensor` | 가상 기온/토양/수액 데이터 (최근 7일 이력 포함) |
| POST | `/api/risk-assessment` | 센서 데이터를 받아 위험도(0~100)와 판단 근거 반환 |
| POST | `/api/analyze-photo` | 과일 사진 + 환경 판정 결과를 받아 Vision LLM으로 착색 진행도를 판독하고, 최종 권고 문장을 합쳐서 반환 |

## 폴더 구조
backend/ FastAPI 앱, 시뮬레이터, 룰 기반 위험도 엔진, OpenRouter LLM 클라이언트
frontend/ Vite + React 대시보드
src/components/
RiskGauge.jsx 히어로 판정 카드 + 음성 출력
PhotoCheck.jsx 과일 사진 업로드 + 미리보기 + Vision LLM 분석
DetailAccordion.jsx 상세 센서 데이터 (접힌 상태 기본값)
SensorChart.jsx 기온/토양수분/수액지수 시계열 차트
StatusBadges.jsx 센서별 상태 요약 뱃지
demo_data/ 심사 시 직접 테스트해볼 수 있는 실제 과일 사진 + 라벨 데이터
(출처: 전북 장수 사과 당도 품질 데이터, AI Hub · 전라북도)


## 데이터 출처
- 과일 사진 판독 검증용 샘플: **전북 장수 사과 당도 품질 데이터** (AI Hub, 전라북도 제공)
  `demo_data/`에 포함. 비상업적 해커톤 데모 목적으로 사용하였습니다.

## 현재 범위 / 다음 단계
- 구현 완료: 룰 기반 위험도 판단, 가상 센서 데이터, OpenRouter Vision LLM 기반 과일 사진 분석
  (모델 자동 폴백 포함), 음성 출력(TTS), 환경·사진 결과 통합 권고 문장
- 다음 단계(예정): 기상청 공공 API 실시간 연동, `caution`/`danger` 단계별 최종 문장 세분화,
  전북 농가 실증 파일럿
두 가지만 더 확인해주세요:

demo_data/ 폴더에 AI Hub 데이터를 그대로 커밋하는 게 라이선스상 괜찮은지 — 비상업 해커톤 데모 용도라 보통 문제없지만, 재배포 조건이 있는지 파일 몇 개만이라도 빠르게 훑어보시는 게 안전합니다.
위 README를 실제 파일에 덮어쓰고 나면, 커밋 메시지에 실명이 안 들어가는지도 한 번 확인하세요 — 익명 제출 규정 때문입니다.
이거 반영되면 이제 구현영상 촬영 시나리오 잡아드릴까요?

구현영상 촬영 시나리오 만들어줘


14초 동안 생각함
14초 동안 생각함
3분 안에 "환경 자동판정 → 사진분석 → 위험 케이스"까지 다 보여줘야 하니, 사전 준비 없이 바로 찍으면 반드시 NG가 납니다. 촬영 전 리허설 한 번은 꼭 하시고, 아래 시나리오대로 가시죠.

사전 준비 (촬영 전 5분, 필수)
 .env에 OpenRouter 키 정상 세팅 확인 (촬영 중 API 실패하면 폴백 문구가 뜨는데, 이건 위험 시나리오가 아니라 "고장난 것처럼" 보여서 안 좋음)
 demo_data/에서 A등급(고당도) 사진 1장, 낮은 등급 사진 1장 미리 골라서 바탕화면에 복사해두기 (파일 탐색기 뒤지는 시간 낭비 안 하게)
 "위험" 케이스를 수동으로 빠르게 만들 값 미리 정해두기 — 예: 현재기온 34, 최근3일평균 31 (risk_engine.py 기준 air_temp_c >= 32 또는 recent_3day_avg_temp_c >= 30이면 확실히 위험으로 뜸)
 화면 녹화 도구: Windows면 Win+G(Xbox Game Bar) 눌러서 캡처 시작 — 마이크 켜서 육성 내레이션 넣을지, 자막만 넣을지 미리 정하기 (시간 없으면 자막 없이 화면만 녹화하고 나중에 텍스트만 얹어도 됨)
 브라우저 새로고침(F5) 몇 번 눌러서 "안전" 결과가 잘 뜨는 시드값 하나 확인해두기 (재현 안 되면 촬영 중 계속 새로고침해야 함)
촬영 스크립트 (총 3분, 여유 있게 2:40~2:50 목표)
시간	화면 동작	자막/내레이션 (원하시면)
0:00–0:15	새 탭에서 localhost:5173 접속, 로딩	"전북 노지 농민 최복남씨(61세) — 오늘도 환상박피를 해야 할지 고민입니다"
0:15–0:35	페이지 로드 완료, 히어로 카드가 자동으로 안전(초록) 판정 표시되는 것을 보여줌	"앱을 열자마자, 별도 조작 없이 오늘의 판정이 바로 나옵니다"
0:35–0:50	"🔊 음성으로 듣기" 버튼 클릭, 실제 음성 재생	"고령 농민을 위해 판정 결과를 음성으로도 들을 수 있습니다"
0:50–1:00	"📸 착색 상태 확인하기" 버튼 클릭, 파일 선택창 뜸	—
1:00–1:10	미리 준비한 사과 사진 선택 (AI Hub 실제 데이터)	"실제 전북 장수 사과 농가 데이터로 테스트합니다"
1:10–1:25	업로드한 사진 미리보기 표시 → "분석 중..." 로딩	—
1:25–1:45	분석 결과 도착, 히어로 카드 문구가 최종 권고 문장으로 바뀌는 것 확인	"Vision AI가 사진을 분석해 환경 판정과 합쳐 최종 결론을 내립니다"
1:45–2:00	"상세 데이터 보기" 아코디언 펼쳐서 기온/토양수분/수액지수 차트 잠깐 보여줌	"판단 근거가 되는 실제 데이터도 확인할 수 있습니다"
2:00–2:15	기온 값을 34, 최근3일평균 31로 수동 입력 → "위험도 판단하기" 클릭	"이번엔 폭염 상황을 가정해보겠습니다"
2:15–2:35	히어로 카드가 빨간색(위험)으로 전환, 경고 문구 + 이유 표시	"위험 상황에서는 명확하게 박피를 만류합니다"
2:35–2:50	(선택) 위험 상태에서도 음성 듣기 버튼 한 번 더 눌러서 마무리	"이상기후 시대, AI가 농민의 안전한 판단을 지켜줍니다"
2:50–3:00	마무리 화면 정지 또는 로고	(끝)
촬영 팁
한 번에 완벽하게 찍으려 하지 마세요. 안전 케이스와 위험 케이스는 각각 따로 짧게 녹화해서, 나중에 편집 없이 그냥 순서대로 파일만 이어붙이면 됩니다 (또는 편집 없이 두 개 영상 중 잘 나온 걸로 골라도 되지만, 규정상 "3분 내외 시연영상 1개"라 어차피 합쳐야 함 — 시간 되면 Windows 기본 '사진' 앱의 비디오 편집기로 자르기/합치기만 해도 충분).
사진 분석 API 호출이 실제로 몇 초 걸릴 수 있으니, 로딩 스피너 구간은 편집으로 살짝 잘라내도 괜찮습니다 (실제 동작임을 보여주는 게 목적이지, 로딩을 그대로 다 보여줄 필요는 없음).
파일명은 참가번호 기준으로, 익명성 확인 (음성 내레이션에 실명 언급 금지, 화면에 개인 정보 없는지 마지막에 한 번 훑어보기).
이대로 찍고 나면 시간이 얼마나 남는지 알려주세요 — 남은 시간에 따라 PPT 최종 점검이나 zip 패키징으로 바로 넘어가겠습니다.



Claude가 응답할 때 알림을 받으시겠습니까?




Claude는 AI이며 실수할 수 있습니다. 응답을 다시 한번 확인해 주세요.




















